@model IEnumerable<Web.ViewModels.Content.PageViewShortModel>

@foreach (var page in Model)
{
    <li>@Html.RouteLink(page.Title, PageRoute.Name, new { area = string.Empty, friendlyUrl = page.FriendlyUrl })</li>
}