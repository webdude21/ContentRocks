<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink(GlobalConstants.SiteName, Actions.Index, Controllers.Home, new { area = string.Empty }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink(Translation.Posts, Actions.Index, Controllers.Posts, new { area = string.Empty }, new { })</li>
                <li>@Html.ActionLink(Translation.Home, Actions.Index, Controllers.Home, new { area = string.Empty }, new { })</li>
                <li>@Html.ActionLink(Translation.About, Actions.About, Controllers.Home, new { area = string.Empty }, new { })</li>
                <li>@Html.ActionLink(Translation.Contact, Actions.Contact, Controllers.Home, new { area = string.Empty }, new { })</li>
                @*@if (User.IsInRole(GlobalConstants.AdministratorRoleName))
                {
                    @Html.Action(Actions.Menu, Controllers.Admin, new { Area = Areas.Administration })
                }*@
            </ul>
            @Html.Partial(Partials.Login)
        </div>
    </div>
</div>